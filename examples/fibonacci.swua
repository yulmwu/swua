extern fn print(int) -> int;

fn fib(n: int) -> int {
    return if n < 2 {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    };
}

fn main() -> int {
    print(fib(10));

    return 0;
}
